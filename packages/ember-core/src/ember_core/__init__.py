"""ember-core — Zero-LLM infrastructure for agent systems.

Subpackages:
    tools     — BaseTool, ToolRegistry, ToolExecutor
    storage   — SQLiteStore (generic, no business tables)
    mcp       — MCPClient, MCPManager, MCP types
    graph     — StateGraph, CompiledGraph, Checkpointer
    pipeline  — PipelineStage, PipelineRunner
    memory    — MemoryStorage (file-level atomic writes, no LLM logic)
"""
